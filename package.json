{
  "devDependencies": {
    "tailwindcss": "^3.4.12"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tailwind": "tailwindcss -i ./src/input.css -o ./src/output.css --watch"
  }
}
